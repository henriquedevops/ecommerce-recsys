# Copie para .env e ajuste conforme o ambiente. O .env NUNCA vai para o git.
# Todas as variáveis são opcionais — sem .env, valem os defaults do config.py.

DATA_DIR=data
MODELS_DIR=models
REPORTS_DIR=reports
MLFLOW_TRACKING_URI=http://localhost:5000
RANDOM_SEED=42
EMBEDDING_DIM=64
BATCH_SIZE=256
LEARNING_RATE=0.001
